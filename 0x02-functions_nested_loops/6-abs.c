#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 * @x: integer input
 * Return: absolute value of ab
 */
int _abs(int x)
{
if (x >= 0)
{
	return (x);
}
else
{
	return (x * -1);
}
}
